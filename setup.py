from setuptools import find_packages, setup

setup(
    name='ecopredict',
    version='0.0.1',
    packages=find_packages(), # This find all folders with __init__.py (like 'src')
    install_requires=[]
)